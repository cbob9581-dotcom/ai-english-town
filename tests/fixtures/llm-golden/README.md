# llm-golden

Actual measurements (TTFT / total latency / tokens) persisted by `llm-smoke.py` after 20 runs with a real key.
They are the basis for pinning `llm_total_timeout_*` / `llm_max_tokens_*` and the source of mock text templates.
Filenames carry a date; not part of CI.
